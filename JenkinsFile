pipeline {
    agent any

    tools {nodejs 'node'}

    stages {
        stage('test'){
            steps{
                sh 'npm install'
            }
        }
        stage('build') {
            steps {
                sh'''
                    docker build
                    docker-compose up
                '''
                    
            }
    }
}
}