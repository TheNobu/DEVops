pipeline {
    agent any

    tools {nodejs 'node'}

    stages {
        stage('test'){
            steps{
                sh 'npm install'
                sh 'npm test'
            }
        }
        stage('build') {
            steps {
                sh 'docker build'
                sh 'docker-compose up'
                
                    
            }
    }
}
}